/**
 * The gameState enum represents the different states of the Asteroids game such as "playing" for the active gameplay
 * state and "end" for the game over state.
 */
public enum gameState {
    playing,end;
}
